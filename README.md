# Todo Web App

A simple Todo app built with **React**, **Vite**, and **Material-UI (MUI)**.  
Todos are stored in **localStorage** and managed with React Hook.

---

## Features

- Add, complete,edit and delete todos
- Filter todos: All, Pending, Completed
- Persistent storage with localStorage
- Responsive UI with Material-UI
